const travels = {
    bindings: {},
    controller: function () {
        let ctrl = this;

        ctrl.$onInit = function () {
        };

    },
    template: `
    `
};

angular.module('portfolio-site.travels')
    .component('travels', travels);

travels.$inject = [];
